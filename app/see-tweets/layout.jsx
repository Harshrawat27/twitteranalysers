export const metadata = {
  title: 'Twitter Profile Tweets',
  description: 'View the oldest 1000 tweets from any Twitter/X profile',
};

export default function Layout({ children }) {
  return <div>{children}</div>;
}
